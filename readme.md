# install `mongosh`

Run the following command:
```
sudo dpkg -i mongosh.deb
```

# Create database 
- use dental_clinic

# Create collections
- db.dentists.insertMany({})
- db.patients.insertMany({})